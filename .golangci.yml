version: "2"

run:
  timeout: 5m
  go: "1.22"

linters:
  enable:
    - misspell
  exclusions:
    paths:
      - bin
      - out
      - samples
      - tmp

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    paths:
      - bin
      - out
      - samples
      - tmp
